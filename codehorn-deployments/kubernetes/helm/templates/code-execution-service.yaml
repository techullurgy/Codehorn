{{- if .Values.codeExecution.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-code-execution-service
  labels:
    app: {{ .Release.Name }}-code-execution-service
spec:
  replicas: {{ .Values.codeExecution.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-code-execution-service
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-code-execution-service
    spec:
      containers:
        # Main CodeExecution Service Container
        - name: main
          image: '{{ .Values.codeExecution.image.repository }}{{ if eq .Values.codeExecution.image.sha "" }}:latest{{ else }}@{{ .Values.codeExecution.image.sha }}{{ end }}'
          imagePullPolicy: {{ .Values.codeExecution.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.codeExecution.service.port }}
              protocol: TCP
          env:
            - name: SPRING_APPLICATION_NAME
              value: "code-execution-service"
            - name: SERVER_PORT
              value: "{{ .Values.codeExecution.service.port }}"
            - name: MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE
              value: "health"
            {{- if .Values.consul.enabled }}
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_HOSTNAME
              value: "{{ .Release.Name }}-code-execution-service"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_PORT
              value: "{{ .Values.codeExecution.service.port }}"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_HEALTH_CHECK_PATH
              value: "/actuator/health"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_HEALTH_CHECK_INTERVAL
              value: "10s"
            {{- else }}
            - name: SPRING_CLOUD_CONSUL_ENABLED
              value: "false"
            {{- end }}
          {{- if .Values.consul.enabled }}
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-consul-config
          {{- end }}
          resources:
            {{- toYaml .Values.codeExecution.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-code-execution-service
  labels:
    app: {{ .Release.Name }}-code-execution-service
spec:
  type: {{ .Values.codeExecution.service.type }}
  ports:
    - port: {{ .Values.codeExecution.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}-code-execution-service
{{- end }}
