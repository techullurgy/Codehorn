{{- if .Values.java.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-java-execution-service
  labels:
    app: {{ .Release.Name }}-java-execution-service
spec:
  replicas: {{ .Values.java.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-java-execution-service
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-java-execution-service
    spec:
      containers:
        # Main Java Execution Service Container
        - name: main
          image: '{{ .Values.java.image.repository }}{{ if eq .Values.java.image.sha "" }}:latest{{ else }}@{{ .Values.java.image.sha }}{{ end }}'
          imagePullPolicy: {{ .Values.java.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.java.service.port }}
              protocol: TCP
          env:
            - name: DOCKER_HOST
              value: tcp://localhost:2375
            - name: SPRING_APPLICATION_NAME
              value: "java-execution-service"
            - name: SERVER_PORT
              value: "{{ .Values.java.service.port }}"
            - name: MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE
              value: "health"
            {{- if .Values.consul.enabled }}
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_HOSTNAME
              value: "{{ .Release.Name }}-java-execution-service"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_PORT
              value: "{{ .Values.java.service.port }}"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_HEALTH_CHECK_PATH
              value: "/actuator/health"
            - name: SPRING_CLOUD_CONSUL_DISCOVERY_HEALTH_CHECK_INTERVAL
              value: "10s"
            {{- else }}
            - name: SPRING_CLOUD_CONSUL_ENABLED
              value: "false"
            {{- end }}
          {{- if .Values.consul.enabled }}
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-consul-config
          {{- end }}
          volumeMounts:
            - name: shared-workspace
              mountPath: /workspace
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - docker image inspect {{ .Values.java.sidecar.runnerImage }} >/dev/null 2>&1
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.java.resources | nindent 12 }}

        # Docker in Docker (DinD) Sidecar Container
        - name: dind-sidecar
          image: "{{ .Values.java.sidecar.image.repository }}:{{ .Values.java.sidecar.image.tag }}"
          imagePullPolicy: {{ .Values.java.sidecar.image.pullPolicy }}
          securityContext:
            privileged: true
          env:
            - name: DOCKER_TLS_CERTDIR
              value: ""
          volumeMounts:
            - name: shared-workspace
              mountPath: /workspace
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - docker image inspect {{ .Values.java.sidecar.runnerImage }} >/dev/null 2>&1
            initialDelaySeconds: 5
            periodSeconds: 5
          lifecycle:
            postStart:
              exec:
                command:
                  - sh
                  - -c
                  - |
                    until docker info >/dev/null 2>&1; do
                      sleep 5
                    done
                    docker pull {{ .Values.java.sidecar.runnerImage }}
      volumes:
        - name: shared-workspace
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-java-execution-service
  labels:
    app: {{ .Release.Name }}-java-execution-service
spec:
  type: {{ .Values.java.service.type }}
  ports:
    - port: {{ .Values.java.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}-java-execution-service
{{- end }}
